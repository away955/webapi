// pub async fn ws_hub(ws: WebSocketUpgrade) -> impl IntoResponse {
//     ws.on_upgrade(|socket| websocket(socket))
// }

// async fn websocket(stream: WebSocket) {
//     let (mut sender, mut receiver) = stream.split();
// }
